header {
    position: relative;
    padding: 1.5rem 2rem 0;
    z-index: 1;
}

main {
    padding: 3rem;
    background: var(--c-gray-light);

    .dark & {
        background: var(--c-gray-darker);
    }
}
